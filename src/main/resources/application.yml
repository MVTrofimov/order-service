spring:
  kafka:
    bootstrap-servers: localhost:9092

app:
  kafka:
    kafkaMessageOrderTopic: "order-topic"
    kafkaMessageOrderStatusTopic: "order-status-topic"
    kafkaMessageGroupId: "order-service-group-id"
